# minicar_ros 作業指示

## 適用範囲と目的

このファイルは `minicar_ros/` 以下の全ファイルに適用する。

このプロジェクトは、ROS 2 Humble と Gazebo Harmonic（gz-sim 8）を Docker 上で動かし、TT-02 相当の `tt02` 車両で Lidar・IMU・SLAM・FTG・MPPI を検証するための実験環境である。変更時は「シミュレータが起動する」だけでなく、センサ、TF、制御、安全停止、オフラインテストの契約を維持すること。

## 最初に確認するもの

- まず `README.md` と、変更対象に直接関係する launch・node・config・SDF を読む。
- `README.md` と実装を現在の正とする。`PLAN.md` は過去の進捗記録を含むため、そこに書かれた「次のタスク」や「未検証」を現状だと決めつけず、実装と `README.md` で確認する。
- このディレクトリは colcon/ament パッケージではない。Python ノードと launch ファイルは bind mount され、コンテナ内では `/nodes`、`/launch`、`/config`、`/worlds` に見える。
- ROS 2 / Gazebo のコマンドは原則コンテナ内で実行する。ホストに同じ依存関係があると仮定しない。

## 変更時に守る契約

- ROS ノードはすべて `use_sim_time:=true` に揃える。`/clock` のブリッジを外さない。
- 安全停止、scan/target timeout、スタック検知、加速度制限を弱める変更は、目的と失敗時挙動を明示し、対応する回帰確認を追加する。

## 実装方針
- "~の実装はどうなっている？"、"~はどうするべきだと思う？"など質問をしたときはまずコードを見て質問にだけ答える。勝手に修正をしたり、実装、検証をしたりしない。
- 検証をするときは、どのような検証をするか提案し、それが承認された場合のみ検証を始める。
- 問題を解決するときは必ず問題の切り分け→問題の対処という段階を踏む。切り分けした結果を推測を含まずこちらに伝えてこれから行うことを決める。AGENT単体で判断をして勝手に実装をしない。
- 実装をするときはplanモードで仕様、テストケースを明確にしてから実装をする
- パラメータはハードコードより `config/*.yaml` を優先する。ただし同一の車両諸元を複数ファイルへコピーしない。
- コメントとドキュメントは日本語を基本とし、単なる処理説明より、物理的根拠、座標系、単位、既知の失敗例、値を変える条件を残す。既存の重要な理由コメントを短縮だけの目的で削除しない。
- 単位を変数名、コメント、パラメータ説明で明確にする。角度は内部では rad、距離は m、速度は m/s を基本とする。
- `worlds/minicar_course.sdf` と `worlds/minicar_course_traced.sdf` は生成物として扱う。コース形状の変更は対応する `generate_course*.py` や `traced.json` を変更して再生成し、生成コマンドと出力の差分を確認する。生成物だけを手編集しない。
- `__pycache__/`、`*.pyc`、一時 CSV、Gazebo/ROS のログを成果物として追加しない。`maps/` は意図した地図出力だけを置く。
- Dockerfile の依存関係を変えた場合は image の rebuild が必要。bind mount 対象の node・launch・config・world だけを変えた場合も、実行中プロセスは必要に応じて再起動する。

## ドキュメント更新と完了条件

- 起動コマンド、既定 world、トピック、frame、パラメータ、測定結果、既知の制約が変わる場合は `README.md` を同じ変更で更新する。
- `PLAN.md` を更新する場合は、完了・未検証・次の作業を現在の実装と整合させ、日付依存の「明日」ではなく具体的な状態で書く。
- README.md,PLAN.mdの内容は極力簡潔にまとめること
- 完了報告には、変更した契約、実行した検証と結果、環境上未実施の確認を簡潔に含める。
